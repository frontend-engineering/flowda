import type { Meta } from '@storybook/react';
import { TheiaDesign } from './theia-design';

const Story: Meta<typeof TheiaDesign> = {
  component: TheiaDesign,
  title: 'TheiaDesign',
};
export default Story;

export const Primary = {
  args: {},
};
